{
  "name": "recurrent-complaints-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Recurrent Complaints Management platform - track recurring complaint themes, their volume history, root causes, product-owner action plans and related incidents.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "reset": "node server/seed.js --force",
    "test": "node --test \"tests/*.test.js\""
  },
  "license": "UNLICENSED"
}
